services: 
 - docker
 
language: java
script: ant build

after_success:
travis env set DOCKER_USERNAME serglazk
travis env set DOCKER_PASSWORD zaq12wsx
  - if [ "$TRAVIS_BRANCH" == "master" ]; then
    docker login -u "$DOCKER_USERNAME" -p "$DOCKER_PASSWORD";
    docker push USER/REPO;
    fi
